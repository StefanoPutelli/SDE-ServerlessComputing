{
  "name": "ex-1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "example": "node src/insertDB.js config/example.toml example && wrangler dev -c config/example.toml --local",
    "ex1": "wrangler dev -c config/ex1.toml --local",
    "ex2": "wrangler dev -c config/ex2.toml --local",
    "ex3": "wrangler dev -c config/ex3.toml --local",
    "deploy_ex1": "wrangler deploy -c config/ex1.toml",
    "deploy_ex2": "wrangler deploy -c config/ex2.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.0.5",
    "wrangler": "^3.60.3"
  }
}
    