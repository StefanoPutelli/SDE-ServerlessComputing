{
  "name": "ex-1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy_example": "wrangler deploy -c Example/wrangler.toml",
    "miniass": "wrangler dev -c MiniAssignment/wrangler.toml --local",
    "deploy_miniass": "wrangler deploy -c MiniAssignment/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "ex-1": "file:"
  }
}
